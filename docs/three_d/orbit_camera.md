# Orbit Camera

マウス操作できる YawPitch カメラ。
数学ライブラリーには、 `DirectXMath` を使う。
後で、 `glTF` をロードする予定なので、Projection には 右手系(RightHanded) を使う。

```{literalinclude} ../../samples/Camera/gorilla/orbit_camera.h
:language: cpp
:caption:
```
